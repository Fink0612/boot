import {pareceres} from './api.js';
import {fatores} from '../fatores/api.js';
import {useConsulta,Consulta,Formulario,Campo} from '../compartilhado/components.jsx';
export default function Pareceres({agenda,podeAvaliar}){const c=useConsulta(()=>Promise.all([fatores.listar(agenda.id),pareceres.listar(agenda.id)]),[agenda.id]);return <><h2>Meus pareceres</h2><p>Informe graus entre 0 e 1. Deixe vazio o grau que ainda não foi avaliado.</p><Consulta consulta={c}>{([lista,avaliacoes])=>lista.length?lista.map(f=>{const p=avaliacoes.find(a=>a.fatorId===f.id)||{};return <section className="card" key={f.id}><h3>{f.titulo}</h3><p>{f.descricao}</p>{podeAvaliar&&agenda.status===2?<Formulario texto="Salvar parecer" onSubmit={async d=>{await pareceres.salvar(f.id,{certeza:d.certeza===''?null:Number(d.certeza),contradicao:d.contradicao===''?null:Number(d.contradicao)});c.recarregar()}}><div className="grid"><Campo label="Certeza" name="certeza" type="number" min="0" max="1" step="0.01" defaultValue={p.certeza??''}/><Campo label="Contradição" name="contradicao" type="number" min="0" max="1" step="0.01" defaultValue={p.contradicao??''}/></div></Formulario>:<p>Certeza: {p.certeza??'Não informada'} · Contradição: {p.contradicao??'Não informada'}</p>}</section>}):<p className="vazio">A agenda ainda não possui fatores.</p>}</Consulta></>}
